use_frameworks!

platform :ios, '10.0'

target 'IQKeyboardToolbarManager_Example' do
  pod 'IQKeyboardToolbarManager', :path => '../'

  target 'IQKeyboardToolbarManager_Tests' do
    inherit! :search_paths

    
  end
end
